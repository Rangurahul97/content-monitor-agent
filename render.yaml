services:
  - type: web
    name: content-monitor-agent
    env: docker
    plan: free # Uses Render's free tier for web services
    # We use a web service to pass Render's health checks and stay free

